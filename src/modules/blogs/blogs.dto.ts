export type BlogsInputDTO = {
    name: string;
    description: string;
    websiteUrl: string;
};

export type BlogsInputRepositoryDTO = {
    id: string;
    name: string;
    description: string;
    websiteUrl: string;
    createdAt: string;
};
